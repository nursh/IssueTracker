import { handleOAuth } from './oauth';
import { handleLocalSignin, handleLocalSignup, handleLogout } from './local';


export {
  handleOAuth,
  handleLocalSignin,
  handleLocalSignup,
  handleLogout
}