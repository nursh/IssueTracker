{
  "name": "issuetracker",
  "version": "1.0.0",
  "description": "IssueTracker application",
  "engines": {
    "node": "10.x",
    "yarn": "1.x"
  },
  "scripts": {
    "build:client": "cd client/ && yarn install  && yarn build",
    "build:server": "npm i --prefix server && npm run build --prefix server",
    "start": "npm start --prefix server",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false YARN_PRODUCTION=false npm run build:server && npm run build:client"
  },
  "cacheDirectories": ["client/node_modules", "server/node_modules"]
}