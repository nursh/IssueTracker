{
  "name": "issuetracker",
  "version": "1.0.0",
  "description": "IssueTracker application",
  "engines": {
    "node": "12.x",
    "yarn": "1.x"
  },
  "scripts": {
    "build:client": "YARN_PRODUCTION=false yarn install --cwd client",
    "build:server": "(NPM_CONFIG_PRODUCTION=false npm i --prefix server && npm run build --prefix server)",
    "start": "npm start --prefix server",
    "postinstall": "npm run build:client && npm run build:server"
  }
}